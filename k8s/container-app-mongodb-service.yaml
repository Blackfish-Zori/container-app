apiVersion: v1
kind: Service
metadata:
  name: mongodb-srv
  labels:
    app: mongodb
spec:
  type: NodePort
  selector:
    app: mongodb
  ports:
    - name: http
      protocol: TCP
      port: 27017
      targetPort: 27017
      nodePort: 30003
