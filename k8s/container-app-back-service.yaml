apiVersion: v1
kind: Service
metadata:
  name: app-back-srv
  labels:
    app: app-back
spec:
  type: NodePort
  selector:
    app: app-back
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30002
