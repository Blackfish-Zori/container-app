apiVersion: v1
kind: Service
metadata:
  name: mongodb
spec:
  type: ExternalName
  # Replace with the actual hostname or IP of your external MongoDB instance.
  # If you only have an IP address (not a DNS name), see the note below —
  # ExternalName requires a DNS name, not a bare IP.
  externalName: your-mongodb-host.example.com
