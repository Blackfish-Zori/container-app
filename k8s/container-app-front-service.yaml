apiVersion: v1
kind: Service
metadata:
  name: app-front-srv
  labels:
    app: app-front
spec:
  type: ClusterIP
  selector:
    app: app-front
  ports:
    - name: http
      protocol: TCP
      port: 8888
      targetPort: 80
