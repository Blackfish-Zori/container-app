apiVersion: v1
kind: Service
metadata:
  name: app-front-srv
  labels:
    app: app-front
spec:
  type: LoadBalancer
  selector:
    app: app-front
  ports:
    - name: http
      protocol: TCP
      port: 8888
      targetPort: 8888
      nodePort: 30888
